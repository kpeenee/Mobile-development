<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="FlashCards.EditPage"
             Title="Edit">
    <ContentPage.Content>
        <StackLayout>
            <Entry x:Name="entTopic" Text="{Binding Topic}"/>
            <Entry x:Name="entQuestion" Text="{Binding Question}"/>
            <Entry x:Name="entAnswer" Text="{Binding Answer}"/>
            <Button Text="Edit" VerticalOptions="EndAndExpand" Clicked="Button_Clicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>